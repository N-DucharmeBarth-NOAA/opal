% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selectivity.R
\name{sel_logistic}
\alias{sel_logistic}
\title{Logistic selectivity as a function of length}
\usage{
sel_logistic(len, par)
}
\arguments{
\item{len}{Numeric vector of length-bin midpoints.}

\item{par}{Numeric vector of length 6 containing selectivity parameters.
Only the first two entries are used: \code{par[1]} (a) and \code{par[2]} (b).
\code{a} is the inflection point on the real line, transformed via
\code{mean(len) + a * sd(len)}, so \code{a = 0} gives inflection at \code{mean(len)}.
\code{b} is log-scale 95\% width, transformed via \code{exp(b) * sd(len)},
so \code{b = 0} gives a 95\% width equal to \code{sd(len)}.}
}
\value{
Numeric vector of selectivity values in (0, 1].
}
\description{
Logistic selectivity as a function of length
}
