% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{get_par_table}
\alias{get_par_table}
\title{Summarise model parameters in a table}
\usage{
get_par_table(
  obj,
  parameters,
  map,
  lower = NULL,
  upper = NULL,
  grad_tol = 1e-04,
  bnds_tol = 0.025,
  include = c("core", "all_est", "all"),
  digits = 3,
  show_map = FALSE
)
}
\arguments{
\item{obj}{An RTMB AD object from \code{MakeADFun}.}

\item{parameters}{Named list of initial parameter values passed to \code{MakeADFun}.}

\item{map}{Named list of map factors passed to \code{MakeADFun}.}

\item{lower}{Numeric vector of lower bounds (length \code{length(obj$par)}),
or \code{NULL} for \code{-Inf} everywhere.}

\item{upper}{Numeric vector of upper bounds (length \code{length(obj$par)}),
or \code{NULL} for \code{Inf} everywhere.}

\item{grad_tol}{Threshold for gradient check: absolute gradient below this
is \code{"OK"}, otherwise \code{"BAD"}. Default \code{1e-4}.}

\item{bnds_tol}{Fraction of finite bound range defining the proximity margin
for the bounds check. Default \code{0.025} (2.5\%).}

\item{include}{Which rows to return: \code{"core"} (default) returns estimated
parameters excluding \code{rdev_y}; \code{"all_est"} includes \code{rdev_y}; \code{"all"}
includes fixed parameters too.}

\item{digits}{Number of significant digits for numeric columns. Default \code{3}.
Use \code{NULL} to suppress rounding.}

\item{show_map}{Logical. Include \code{map} and \code{fixed} columns. Default \code{FALSE}.}
}
\value{
A data.frame with columns \code{par}, \code{init}, \code{est}, \code{lwr},
\code{upr}, \code{grd}, \code{gr_chk}, \code{bd_chk}, and optionally \code{group}, \code{map}
and \code{fixed} (when \code{show_map = TRUE}).
}
\description{
Builds a data.frame combining initial values, estimated values, bounds,
gradient, gradient check, and bounds check for all parameters. Each row
is one scalar element of the unlisted parameter vector.
}
