% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selectivity.R
\name{sel_double_richards}
\alias{sel_double_richards}
\title{Double Richards selectivity as a function of length}
\usage{
sel_double_richards(len, par)
}
\arguments{
\item{len}{Numeric vector of length-bin midpoints.}

\item{par}{Numeric vector of length 6 containing selectivity parameters.}
}
\value{
Numeric vector of selectivity values in [0, 1).
}
\description{
A smooth, six-parameter curve formed by multiplying an ascending
generalized-logistic (Richards) limb by one minus a descending Richards
limb:
\deqn{(1 + \exp(-z_1))^{-1/\gamma_1}
[1 - (1 + \exp(-z_2))^{-1/\gamma_2}].}
Parameters are on the real line and transformed as follows:
\describe{
\item{\code{par[1]}}{Ascending 50\% point: \code{mean(len) + par[1] * sd(len)}.}
\item{\code{par[2]}}{Ascending width: \code{exp(par[2]) * sd(len)}.}
\item{\code{par[3]}}{Ascending shape: \code{gamma1 = exp(par[3])}.}
\item{\code{par[4]}}{Descending 50\% point is ascending 50\% point plus
\code{exp(par[4]) * sd(len)}.}
\item{\code{par[5]}}{Descending width: \code{exp(par[5]) * sd(len)}.}
\item{\code{par[6]}}{Descending shape: \code{gamma2 = exp(par[6])}.}
}
The Maunder parameterization is recovered with
\code{alpha_k = L50_k + log(2^gamma_k - 1) / beta_k} and
\code{beta_k = log(19) / width_k}. The peak can be below one when the limbs
overlap; it is intentionally not rescaled. As either shape parameter tends
to negative infinity, its limb tends to a Gompertz curve with the same 50\%
point. Large shape values flatten the limb toward 0.5 (at \code{par[3] = par[6] = 7}, the curve is approximately 0.25). Mapping \code{par[3]} and
\code{par[6]} to zero gives a four-parameter double-logistic curve. Suggested
bounds for columns 3 and 6 are \code{c(-5, 5)}; use the existing \code{par_sel} bounds
for the remaining columns.
}
\references{
Maunder, M. (2025). Double Richards selectivity. Unpublished
technical note, 19 November 2025.
}
