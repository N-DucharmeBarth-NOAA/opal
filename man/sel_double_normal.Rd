% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selectivity.R
\name{sel_double_normal}
\alias{sel_double_normal}
\title{Double-normal selectivity as a function of length (SS3 pattern 24, full form)}
\usage{
sel_double_normal(x, par)
}
\arguments{
\item{x}{Numeric vector of length-bin midpoints.}

\item{par}{Numeric vector of length 6 containing selectivity parameters.
\describe{
\item{\code{par[1]} (a)}{Peak location (real line). Transformed via
\code{mean(x) + a * sd(x)}, so \code{a = 0} places the peak at \code{mean(x)}.}
\item{\code{par[2]} (b)}{Plateau width (real line). Controls the distance from peak to the
start of the descending limb via logistic transform of the available
range: \code{peak + bin_width + (0.99 * max(x) - peak - bin_width) / (1 + exp(-b))}.}
\item{\code{par[3]} (c)}{Ascending width (real line, log-space). Actual width = \code{exp(c) * sd(x)}.
\code{c = 0} gives an ascending width equal to \code{sd(x)}.}
\item{\code{par[4]} (d)}{Descending width (real line, log-space). Actual width = \code{exp(d) * sd(x)}.
\code{d = 0} gives a descending width equal to \code{sd(x)}.}
\item{\code{par[5]} (e)}{Initial selectivity (real line, logit-space). Transformed via
\code{1 / (1 + exp(-e))}, so \code{e = 0} gives initial selectivity of 0.5,
large negative values give ~0, large positive values give ~1.}
\item{\code{par[6]} (f)}{Final selectivity (real line, logit-space). Same transform as \code{e}.}
}}
}
\value{
Numeric vector of selectivity values in [0, 1].
}
\description{
All parameters are on the real line, making this parameterization suitable
for unconstrained optimization and AD. The length-bin vector \code{x} is used
to define natural scales for position and width parameters.
}
\details{
Only the full form is implemented (e and f both active). The simplified
forms where e <= -999 or f <= -999 are not supported.
}
