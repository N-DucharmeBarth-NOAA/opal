% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{resolve_bio_vector}
\alias{resolve_bio_vector}
\title{Resolve a biology vector to age-basis}
\usage{
resolve_bio_vector(vec, n_age, n_len, pla, name = "vector")
}
\arguments{
\item{vec}{Numeric vector (length \code{n_age} or \code{n_len}).
May be an AD type inside \code{bet_model()}.}

\item{n_age}{Integer.  Number of age classes.}

\item{n_len}{Integer.  Number of length bins.}

\item{pla}{Matrix (\code{n_len} x \code{n_age}).  Probability of length at
age (columns sum to 1), as returned by \code{\link{get_pla}}.}

\item{name}{Character.  Name of the vector used in warning/error messages.}
}
\value{
Numeric vector of length \code{n_age} on an age basis.
}
\description{
If the vector length matches \code{n_age}, it is returned unchanged
(assumed to already be on an age basis).  If the vector length matches
\code{n_len} (and \code{n_len != n_age}), it is converted to age using the
probability-of-length-at-age matrix: \code{vec_a = t(pla) \%*\% vec_l}.
Otherwise an informative error is thrown.
}
\details{
\strong{Note:} When \code{n_age == n_len} the vector is always treated as
age-basis.  If you need to pass a length-basis vector in that situation you
must convert it to age externally before calling the model.
}
