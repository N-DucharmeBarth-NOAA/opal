% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/growth.R
\name{get_recruit_length_dist}
\alias{get_recruit_length_dist}
\title{Recruit length distribution}
\usage{
get_recruit_length_dist(len_lower, len_upper, mu_r, sd_r)
}
\arguments{
\item{len_lower}{Numeric vector of lower bounds of length bins (length n_len).}

\item{len_upper}{Numeric vector of upper bounds of length bins (length n_len).}

\item{mu_r}{Numeric. Mean length at recruitment (may be AD).}

\item{sd_r}{Numeric. SD of length at recruitment (may be AD).}
}
\value{
Numeric vector (length n_len) summing to 1.
}
\description{
Distribution of recruits across length bins, assuming length-at-recruitment
is normal with mean \code{mu_r} and SD \code{sd_r}. Matches the first column
of \code{\link{get_pla}} when \code{mu_r = mu_a[1]} and \code{sd_r = sd_a[1]}.
AD-compatible (uses \code{RTMB::pnorm}) so gradients propagate to growth
parameters when they are estimated.
}
