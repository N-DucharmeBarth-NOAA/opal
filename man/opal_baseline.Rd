% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{opal_baseline}
\alias{opal_baseline}
\title{\code{opal} model regression baseline}
\format{
A named list accessible via \code{data(opal_baseline)} and
packaged as \code{data/opal_baseline.rda}.
}
\source{
\code{vignettes/baseline.Rmd} (the "Save or compare" chunk) which
reproduces the reference run saved to \code{data/opal_baseline.rda}.
}
\usage{
opal_baseline
}
\description{
A saved reference run for the WCPO bigeye tuna assessment that documents the
objective, gradient, and RTMB report output used by the
\code{vignettes/baseline.Rmd} workflow to detect numerical regressions and timing
changes before and after refactors.
}
\details{
\subsection{Baseline contents}{

\describe{
\item{timestamp}{Time when the baseline was created.}
\item{description}{Free-form label ("Pre-refactoring baseline" by default).}
\item{opal_version}{Version string of the installed \code{opal} package.}
\item{dimensions}{Named list with \code{n_year}, \code{n_age}, \code{n_fishery}, \code{n_season},
\code{n_len}, \code{n_lf}, \code{n_wt}, and \code{n_wf} from the model inputs.}
\item{nll}{Negative log-likelihood at the reference parameter set.}
\item{gradient}{Gradient vector evaluated at \code{obj$par} for the baseline.}
\item{max_gr}{Maximum absolute gradient entry.}
\item{timing}{List with \code{fn_elapsed}, \code{gr_elapsed}, \code{fn_median}, and
\code{gr_median} values that summarize runtime measurements used in the
comparison vignette.}
\item{report}{Sublist containing the baseline's \code{number_ysa},
\code{spawning_biomass_y}, catch and harvest rate predictions (\code{catch_pred_fya},
\code{catch_pred_ysf}, \code{hrate_ysa}, \code{hrate_ysfa}), selectivity curves
(\code{sel_fya}), and log-likelihood components (\code{lp_prior}, \code{lp_penalty},
\code{lp_rec}, \code{lp_cpue}, \code{lp_lf}, \code{lp_wf}).}
\item{par_values}{Vector of \code{obj$par} values that produced the baseline.}
}
}
}
\examples{
\dontrun{
  data(opal_baseline)
  str(opal_baseline$dimensions)
  opal_baseline$timing$fn_median
}

}
\keyword{datasets}
