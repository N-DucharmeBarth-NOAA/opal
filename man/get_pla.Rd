% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selectivity.R
\name{get_pla}
\alias{get_pla}
\title{Probability of length at age matrix (age-length key)}
\usage{
get_pla(len_lower, len_upper, mu_a, sd_a)
}
\arguments{
\item{len_lower}{Numeric vector of lower bounds of length bins (length L). Data only.}

\item{len_upper}{Numeric vector of upper bounds of length bins (length L). Data only.}

\item{mu_a}{Numeric vector of mean length at age (length A). May be AD.}

\item{sd_a}{Numeric vector of SD of length at age (length A). May be AD.}
}
\value{
Matrix of dimensions L x A where columns sum to 1.
}
\description{
Computes the probability distribution of fish lengths across different
age classes. AD-compatible: uses RTMB::pnorm and ADoverload so that
gradients propagate if growth parameters (mu_a, sd_a) are estimated.
}
\details{
Bins are assumed contiguous and non-overlapping, i.e.
\code{len_upper[i] == len_lower[i + 1]} for all \code{i}. This allows a
single vectorized \code{pnorm} call per age on the \code{n_len + 1} unique
bin edges rather than two calls on the lower and upper bounds separately.
}
