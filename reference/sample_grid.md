# Sample from the grid

Sample from the grid

## Usage

``` r
sample_grid(grid, n_samples = 2000)
```

## Arguments

- grid:

  a `list` of model runs.

- n_samples:

  the number of samples from the grid.

## Value

a `vector` of integers.
