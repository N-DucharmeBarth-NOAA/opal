# Validate a portable opal fit

Performs structural validation without rebuilding the RTMB objective.

## Usage

``` r
validate_opal_fit(x)
```

## Arguments

- x:

  Object expected to inherit from `opal_fit`.

## Value

`x`, invisibly, when valid.
