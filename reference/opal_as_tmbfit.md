# Convert normalized opal posterior draws to a tmbfit

Convert normalized opal posterior draws to a tmbfit

## Usage

``` r
opal_as_tmbfit(x)
```

## Arguments

- x:

  An `opal_fit` with MCMC output or an `opal_mcmc` object.

## Value

A list inheriting from `tmbfit`.
