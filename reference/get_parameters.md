# Get default initial parameter values

Get `list` of default initial parameter values.

## Usage

``` r
get_parameters(data)
```

## Arguments

- data:

  a `list` containing the data inputs.

## Value

a `list` of initial parameter values.
