# The opal model

Obtain the negative log-likelihood (NLL) value from the opal model.

## Usage

``` r
opal_model(parameters, data)
```

## Arguments

- parameters:

  a `list` of parameter values.

- data:

  a `list` of data inputs.

## Value

the negative log-likelihood (NLL) value.
