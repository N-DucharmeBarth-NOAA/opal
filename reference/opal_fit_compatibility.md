# Check compatibility of a saved opal fit

Check compatibility of a saved opal fit

## Usage

``` r
opal_fit_compatibility(x)
```

## Arguments

- x:

  An `opal_fit` object.

## Value

A list containing `compatible`, `errors`, and `warnings`.
