# Recreate the fitted model report

Recreate the fitted model report

## Usage

``` r
opal_fit_report(x)
```

## Arguments

- x:

  An `opal_fit` object.

## Value

The named report list returned by `opal_model`.
