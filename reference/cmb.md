# Helper to make closure

Helper to make closure

## Usage

``` r
cmb(f, d)
```

## Arguments

- f:

  a `vector` of midpoints.

- d:

  natural mortality at the reference size.

## Value

a `vector`.
