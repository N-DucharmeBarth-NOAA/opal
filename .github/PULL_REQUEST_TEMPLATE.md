## What this changes

<!-- Brief description, and the issue this closes (e.g. "Closes #123"). -->

## Numerical impact

<!-- Does this change model output? If so, state which quantities and by how much.
     If it is numerically inert, say so explicitly. -->

- [ ] No change to model output
- [ ] Changes model output (described above)

## Checklist

- [ ] `devtools::check()` passes locally
- [ ] Regression / golden-output tests updated if outputs changed, with the reason recorded
- [ ] Backward compatibility considered, and any breaking change documented
- [ ] Roxygen documentation regenerated if signatures changed
